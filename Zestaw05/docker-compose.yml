services:
  backend:
    build: ./server
    container_name: shop-backend
    ports:
      - "8080:8080"

  frontend:
    build: ./shop-client
    container_name: shop-frontend
    ports:
      - "5173:5173"
    depends_on:
      - backend